## Usage

```js
const { progress } = require("oxy-progress-bar1");
const bar = "━";
const total = 100;
const current = 10;
const size = 10;
const slider = "❤️"

progress(bar,current, total,slider,size)[0]
```
