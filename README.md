<h2>Usage<h2>

```js
const { progress } = require("music-progress-bar");
const bar = "━";
const total = "100";
const current = "10";
const size = 10;
const slider = "❤️"

progress(bar,current, total,slider,size)

```
