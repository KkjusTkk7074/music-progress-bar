<h2>Usage<h2>

```js
const { progress } = require("music-progress-bar");
const bar = "━";
const total = "";
const current = "";
const size = 20;
const slider = "❤️"

progress(bar,current, total,slider,size=11)

```
